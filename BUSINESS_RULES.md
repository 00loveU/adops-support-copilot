# AdOps Support Copilot 指标与异常诊断规则 V0.1

> 状态：**已确认（2026-09-09）**  
> 用途：定义第一版指标口径、最小样本要求和模拟异常阈值  
> 重要说明：以下阈值仅用于个人演示项目的模拟数据，不代表任何真实广告平台或公司的正式规则。本版本已经用户确认，可以用于模拟数据设计与第一版实现；后续修改仍需重新确认。

## 1. 指标口径

### 1.1 原始指标

| 指标 | 字段 | 单位 | 说明 |
|---|---|---:|---|
| 曝光量 | `impressions` | 次 | 广告被展示的次数 |
| 点击量 | `clicks` | 次 | 广告被点击的次数 |
| 转化量 | `conversions` | 次 | 完成目标行为的次数 |
| 广告消耗 | `spend_cents` | 分 | 广告投放消耗 |
| 转化收入 | `revenue_cents` | 分 | 归因到广告的模拟收入 |

### 1.2 计算指标

| 指标 | 公式 | 无法计算条件 |
|---|---|---|
| CTR | 点击量 ÷ 曝光量 | 曝光量为 0 |
| CVR | 转化量 ÷ 点击量 | 点击量为 0 |
| CPC | 广告消耗 ÷ 点击量 | 点击量为 0 |
| CPA | 广告消耗 ÷ 转化量 | 转化量为 0 |
| ROAS | 转化收入 ÷ 广告消耗 | 广告消耗为 0 |

统一约定：

- CTR、CVR 在内部使用小数，页面展示为百分比。
- CPC、CPA 使用整数分计算，页面展示为人民币元。
- ROAS 是广告投产比，不称为净 ROI。
- 查询多天数据时，先汇总原始指标，再根据汇总值计算比率；不使用每日比率的简单平均值。
- 分母为 0 时返回 `null` 和原因，不返回 0、无穷大或估算值。

### 1.3 精度与舍入规则

- 多日查询先汇总曝光、点击、转化、消耗和收入等原始整数，再计算派生指标。
- 后端使用十进制高精度计算，不使用二进制浮点数参与指标计算和阈值判断。
- 异常阈值使用未舍入的精确计算结果判断，不能使用页面展示值判断。
- API 中 CTR、CVR、ROAS 最多保留 6 位小数。
- 页面中的 CTR、CVR 转为百分比后保留 2 位小数。
- CPC、CPA 舍入到整数分，采用四舍五入（`ROUND_HALF_UP`）。
- 分母为 0 时指标返回 `null`，并同时返回明确的不可计算原因。

## 2. 诊断输出规则

一次诊断必须区分四类内容：

1. `facts`：由数据直接确认的事实。
2. `anomalies`：满足阈值的模拟异常规则。
3. `possible_causes`：尚未验证的可能原因。
4. `suggestions`：建议运营人员人工检查的步骤。

系统不能把 `possible_causes` 表达成已经确认的事实，也不能执行广告修改。

## 3. 最小样本规则

为了避免用极少数据作出强结论，第一版采用以下最小样本：

| 判断项 | 最小样本 |
|---|---|
| CTR 异常 | 曝光量至少 1,000 |
| CVR 异常 | 点击量至少 100 |
| 有点击无转化 | 点击量至少 100，且消耗至少 300 元 |
| CPA 异常 | 转化量至少 5 |
| ROAS 异常 | 消耗至少 300 元 |
| 消耗波动 | 前 7 天至少有 5 天有效数据，且日均消耗至少 100 元 |

消耗波动中的“有效数据日”统一定义为：当天存在指标记录，并且 `spend_cents > 0`。基准日均消耗只使用这些有效数据日计算，不把零消耗日计入平均值。

未达到最小样本时：

- 仍然返回事实和指标。
- 不命中对应异常规则。
- 提示“样本量不足，暂不作异常判断”。
- API 将这类提示单独放入诊断结果的 `sample_warnings`，不混入 `anomalies`。

## 4. 第一版异常规则

### 4.1 `ANOM-NO-IMPRESSION-001`：启用计划无曝光

触发条件：

- 广告计划状态为 `active`。
- 查询范围内曝光量等于 0。

严重程度：`high`

可能原因：

- 计划尚未通过审核。
- 预算或投放条件限制过严。
- 计划虽然标记为启用，但实际投放条件不满足。

建议：

- 检查审核状态。
- 检查预算、日期和投放条件。
- 检查模拟数据中的计划状态是否一致。

关联知识来源：`RULE-DELIVERY-001`

### 4.2 `ANOM-LOW-CTR-001`：点击率偏低

前置条件：曝光量至少 1,000。

阈值：

| 条件 | 严重程度 |
|---|---|
| `CTR < 0.5%` | `high` |
| `0.5% <= CTR < 1.0%` | `medium` |
| `CTR >= 1.0%` | 不触发 |

可能原因：

- 广告素材吸引力不足。
- 素材与目标人群不匹配。
- 广告卖点表达不清晰。

建议：

- 检查素材与目标人群的一致性。
- 对比不同素材的点击表现。
- 优先检查标题、主图和核心卖点。

关联知识来源：`RULE-CTR-001`

### 4.3 `ANOM-NO-CONVERSION-001`：有点击但无转化

触发条件：

- 点击量至少 100。
- 广告消耗至少 300 元。
- 转化量等于 0。

严重程度：`high`

可能原因：

- 落地页无法正常访问或完成转化。
- 广告内容与落地页信息不一致。
- 转化回传配置异常。
- 商品价格或转化路径存在问题。

建议：

- 检查落地页可用性。
- 核对广告素材和落地页内容。
- 检查转化回传链路。
- 人工检查商品状态和转化路径。

关联知识来源：`RULE-CVR-001`

互斥规则：命中本规则时，不重复命中“CVR 偏低”；CPA 因分母为 0，仅返回不可计算。

### 4.4 `ANOM-LOW-CVR-001`：转化率偏低

前置条件：

- 点击量至少 100。
- 转化量大于 0。

阈值：

| 条件 | 严重程度 |
|---|---|
| `CVR < 0.5%` | `high` |
| `0.5% <= CVR < 1.0%` | `medium` |
| `CVR >= 1.0%` | 不触发 |

可能原因：

- 落地页体验较差。
- 广告承诺与实际商品不一致。
- 商品价格、库存或购买路径存在问题。

建议：

- 检查落地页加载和购买流程。
- 核对广告信息与商品信息。
- 对比不同日期或计划的 CVR。

关联知识来源：`RULE-CVR-001`

### 4.5 `ANOM-HIGH-CPA-001`：转化成本偏高

前置条件：转化量至少 5。

阈值：

| 条件 | 严重程度 |
|---|---|
| `CPA > 120 元` | `high` |
| `80 元 < CPA <= 120 元` | `medium` |
| `CPA <= 80 元` | 不触发 |

可能原因：

- 点击成本偏高。
- 转化率偏低。
- 当前计划获得的转化量不足以摊薄消耗。

建议：

- 同时检查 CPC 和 CVR。
- 对比同广告主其他计划的 CPA。
- 人工评估目标 CPA 是否与业务目标一致。

关联知识来源：`RULE-CPA-001`

### 4.6 `ANOM-LOW-ROAS-001`：广告投产比偏低

前置条件：广告消耗至少 300 元。

阈值：

| 条件 | 严重程度 |
|---|---|
| `ROAS < 0.8` | `high` |
| `0.8 <= ROAS < 1.2` | `medium` |
| `ROAS >= 1.2` | 不触发 |

可能原因：

- 转化量不足。
- 单次转化收入较低。
- 广告消耗增长快于转化收入。

建议：

- 结合 CVR、CPA 和转化收入排查。
- 对比同广告主其他计划的 ROAS。
- 由于缺少商品成本，不能据此直接判断真实利润。

关联知识来源：`RULE-ROAS-001`

### 4.7 `ANOM-SPEND-CHANGE-001`：单日消耗异常波动

适用范围：仅分析单日数据时执行。

基准：分析日期之前 7 天的平均日消耗。

前置条件：

- 前 7 天至少有 5 天有效数据。
- 基准日均消耗至少 100 元。

阈值：

| 条件 | 异常类型 | 严重程度 |
|---|---|---|
| 当日消耗 `> 1.5 ×` 基准日均消耗 | 消耗突然上升 | `medium` |
| 当日消耗 `< 0.5 ×` 基准日均消耗 | 消耗突然下降 | `medium` |
| 其他 | 不触发 | - |

可能原因：

- 预算或投放状态变化。
- 流量环境变化。
- 素材、定向或审核状态变化。

建议：

- 对比计划状态和预算。
- 检查曝光量、点击量是否同步变化。
- 结合其他指标判断，而不是只根据消耗下结论。

关联知识来源：`RULE-SPEND-001`

## 5. 多规则同时命中

第一版允许同一计划命中多个有解释关系的规则，例如：

```text
CVR 偏低
  ↓
CPA 偏高
  ↓
ROAS 偏低
```

展示顺序：

1. 无曝光。
2. 有点击无转化。
3. CTR 或 CVR 偏低。
4. CPA 偏高。
5. ROAS 偏低。
6. 消耗波动。

互斥处理：

- 无曝光时不判断 CTR、CVR、CPC、CPA。
- 点击量为 0 时不判断 CVR、CPC、CPA。
- 命中“有点击无转化”时不重复命中“CVR 偏低”。
- 转化量为 0 时 CPA 只返回不可计算，不判断 CPA 偏高。

## 6. 第一版规则验收样例

| 样例 | 核心数据 | 预期结果 |
|---|---|---|
| A | 启用计划，曝光 0 | 命中无曝光 |
| B | 曝光 10,000，点击 40 | CTR 0.4%，命中高严重度低 CTR |
| C | 点击 150，消耗 400 元，转化 0 | 命中有点击无转化，不重复命中低 CVR |
| D | 点击 200，转化 1 | CVR 0.5%，命中中严重度低 CVR |
| E | 转化 5，消耗 650 元 | CPA 130 元，命中高严重度高 CPA |
| F | 消耗 500 元，收入 350 元 | ROAS 0.7，命中高严重度低 ROAS |
| G | 前 7 天日均 200 元，当日 320 元 | 1.6 倍，命中消耗突然上升 |
| H | 曝光 500，CTR 0.2% | 样本不足，不判断低 CTR |

## 7. 确认记录

- 确认日期：2026-09-09。
- 确认范围：指标口径、精度与舍入规则、最小样本、有效数据日定义、`sample_warnings` 输出、7 类异常规则、阈值、严重程度、互斥关系和验收样例。
- 使用边界：只作为模拟演示规则，不代表真实广告平台或公司的正式标准。
- 变更流程：后续新增或修改任何规则后，必须再次暂停并取得用户确认，才能用于数据或代码。
