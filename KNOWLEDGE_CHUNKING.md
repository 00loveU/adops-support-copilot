# AdOps Support Copilot 知识分块与检索设计 V1.0

> 内容依据：[KNOWLEDGE_RULES.md](./KNOWLEDGE_RULES.md) 已确认版本  
> 第一版目标：使用 SQLite FTS5 检索 7 份中文模拟 SOP，并返回可核对来源

## 1. 文档格式

每个知识来源保存为一份 Markdown 文件。文件顶部包含：

```yaml
source_code: RULE-CTR-001
title: 点击率偏低排查
category: ctr
version: "1.0"
keywords: CTR 点击率低 曝光多点击少 素材 人群不匹配
```

正文使用二级标题划分语义完整的小节，例如：

- 适用范围。
- 排查步骤。
- 可能原因。
- 回答边界。

## 2. 分块规则

第一版按 Markdown 二级标题 `##` 分块：

```text
一份知识文档
├─ 适用范围块
├─ 排查步骤块
├─ 可能原因块
└─ 回答边界块
```

每个片段保存：

- `source_code`
- `title`
- `category`
- `version`
- `keywords`
- `section_title`
- `content`
- `chunk_order`

不使用固定字符数强行切分，因为当前文档很短，标题分块能够保留完整排查语义。只有单个小节未来明显过长时，才在段落边界继续拆分。

## 3. 中文全文检索

第一版使用 SQLite FTS5 的 `trigram` tokenizer，为连续中文文本提供三字符子串匹配。例如用户查询“没有曝光”，可以命中文档中的“广告计划没有曝光”。

索引内容包括：

- 来源标题。
- 小节标题。
- 小节正文。
- 已确认的同义检索词。

`source_code`、`category` 和 `version` 作为元数据保存，不参与自然语言生成。`keywords` 只用于召回，不作为回答依据展示。

## 4. 查询处理

第一版只做最小处理：

1. 去除首尾空白。
2. 将英文指标名统一为大写或小写一致形式。
3. 根据已确认文档中的 `keywords`，把命中的同义表达转换为规范检索短语。
4. 没有命中已知同义词时，保留用户原始中文关键词。
5. 使用 FTS5 查询。
6. 按相关性返回最多 3 个片段。
7. 多个片段来自同一来源时允许合并展示。

第一版不增加 LLM 查询改写、向量检索或 Reranker。只有固定评估表明中文召回不足时再升级。

## 5. 来源引用

传给回答生成模块的每个片段必须携带真实元数据：

```json
{
  "source_code": "RULE-CVR-001",
  "title": "转化率异常排查",
  "section_title": "排查步骤",
  "content": "..."
}
```

最终回答只能展示这些实际返回的来源。模型输出了未检索到的来源编号时，后端需要丢弃该编号。

## 6. 无结果处理

FTS5 没有返回可靠片段时：

- 不调用模型常识补写平台规则。
- 返回“当前模拟知识库没有该问题的可靠依据”。
- 告知用户第一版支持的知识范围。

## 7. 最小检索验收

| 用户问题示例 | 规范检索短语 | 预期首要来源 |
|---|---|---|
| CTR 是什么意思 | 指标口径 | RULE-METRIC-001 |
| 广告计划没有曝光 | 没有曝光 | RULE-DELIVERY-001 |
| 曝光很多但是点击很少 | 点击很少 | RULE-CTR-001 |
| 有点击但是没有转化 | 没有转化 | RULE-CVR-001 |
| 转化成本太高 | 转化成本 | RULE-CPA-001 |
| 广告投产比很低 | 广告投产比 | RULE-ROAS-001 |
| 昨天消耗突然上涨 | 消耗突然上涨 | RULE-SPEND-001 |

## 8. 暂不实现

- Qdrant。
- Embedding。
- 混合检索。
- 查询改写。
- Reranker。
- 文档在线编辑。
- 文档自动同步。

这些能力只有在第一版检索评估出现明确问题后再考虑。
